{
  "ConnectionStrings": {
    "Database": "Host=localhost;Database=modulartemplate_dev;Username=postgres;Password=postgres",
    "Cache": "localhost:6379"
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "http://localhost:5173",
      "https://localhost:3000",
      "https://localhost:5173"
    ]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft": "Information",
      "Microsoft.EntityFrameworkCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "Resilience": {
    "Retry": {
      "MaxRetryAttempts": 2,
      "BaseDelayMilliseconds": 500,
      "UseJitter": true
    },
    "CircuitBreaker": {
      "SamplingDurationSeconds": 5,
      "FailureRatio": 0.3,
      "MinimumThroughput": 2,
      "BreakDurationSeconds": 10
    },
    "Timeout": {
      "TotalTimeoutSeconds": 10,
      "AttemptTimeoutSeconds": 3
    }
  }
  /*To use SEPARATE DATABASES per module locally, uncomment the Modules section below and remove the underscore prefix. Each module will then connect to its own database instead of sharing one database with separate schemas.*/
  /*"Modules": {
    "SampleSales": {
      "ConnectionStrings": {
        "Database": "Host=localhost;Database=samplesales_dev;Username=postgres;Password=postgres"
      }
    },
    "SampleOrders": {
      "ConnectionStrings": {
        "Database": "Host=localhost;Database=sampleorders_dev;Username=postgres;Password=postgres"
      }
    },
    "Organization": {
      "ConnectionStrings": {
        "Database": "Host=localhost;Database=organization_dev;Username=postgres;Password=postgres"
      }
    },
    "Customer": {
      "ConnectionStrings": {
        "Database": "Host=localhost;Database=customer_dev;Username=postgres;Password=postgres"
      }
    },
    "Sales": {
      "ConnectionStrings": {
        "Database": "Host=localhost;Database=sales_dev;Username=postgres;Password=postgres"
      }
    }
  }  */
}
